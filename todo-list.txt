My project structure

apps/
├── student/           # Student registration, fix requests, reviews
├── service/          # Service staff completing fixes
├── maintenance/       # Fix request system (shared)
└── donations/         # Donation system

    #abstraction
    email = models.EmailField(unique=True)
    username = models.CharField(max_length=50, unique=True)
    phone = models.CharField(max_length=20, blank=True)
    user_type = models.CharField(max_length=10, choices=USER_TYPE_CHOICES)

    # priority must be handled with the admin
    # 404.html must be used in the end of the project
    # 4 building 2 floor 3 room  4*2*3 = 24 room in totall

#######################
delete the main page
show the services
fix the login active student in register
########################
superuser
    - email: Babakfathi082@gmail.com
    - username: Babak
    -password: Babakft82#